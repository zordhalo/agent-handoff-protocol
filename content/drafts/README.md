# Drafts

Content drafted by the personas in [`docs/TEAM.md`](../../docs/TEAM.md).
Nothing in this folder is published — see that doc for the workflow and why
that boundary is a hard rule here, not a formality.

Each file's front matter tracks status: `draft` → `approved` / `revised` /
`dropped`, set by a human, never by a persona.
